
services:
  app:
    build: .
    image: visionwise:1.0  # Specify the image name and tag
    ports:
      - "8501:8501"
    volumes:
      - .:/app
      - app_data:/app/data 
    command: ["streamlit", "run", "app.py"]

volumes:
  app_data: